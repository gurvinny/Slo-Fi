# Generated by dashboard discovery engine
# Vite SPA project: slo-fi

services:
  slo-fi:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: slo-fi
    restart: unless-stopped
    ports:
      - "5173:5173"
    labels:
      dashboard.project: "slo-fi"
      dashboard.type: "vite"
      dashboard.port: "5173"
